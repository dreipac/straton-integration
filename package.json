{
  "name": "straton-integration",
  "private": true,
  "workspaces": [
    "apps/*", "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w apps/login",
    "dev:login": "npm run dev -w apps/login"
  }
}